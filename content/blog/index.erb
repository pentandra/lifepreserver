--- 
title: Blog
description: A collection of our thoughts about Pentandra and the future of research.
---
<div class="group" prefix="<%= prefix_mappings_for(:sioct) %>" resource="pentandra-blog:blog" typeof="schema:Blog sioct:Weblog">

  <h1 property="schema:name sioc:name"><%= @config[:blog][:title] %></h1>

  <div resource="pentandra-blog:articles" typeof="schema:ItemList">
    <link property="schema:itemListOrder" resource="schema:itemListOrderDescending" />
    <meta property="schema:numberOfItems" content="<%= h published_articles.length %>" datatype="schema:Integer" />
    <% published_articles.each_with_index do |article, index| %>
      <%= render('/blog/partials/summary.*', item: article, position: index + 1) %>
    <% end %>
  </div>

  <link property="schema:isPartOf" resource="pentandra-website:website" />

</div>
