<% render('/base.*') do %>

  <%= render '/partials/header.*' %>

  <main role="main">
    <div class="content">

      <%= render('/partials/wip.*') if @item[:wip] %>

      <%= yield %>

    </div>
  </main>

  <%= render('/partials/footer.*') %>

<% end %>
