<ul class="none" property="schema:isBasedOn" resource="pentandra-website:project" typeof="doap:Project">

  <li>
    <span property="doap:repository" resource="pentandra-website:repository" typeof="doap:GitRepository schema:SoftwareSourceCode">
      <a rel="doap:location schema:codeRepository" property="schema:name doap:shortdesc" href="<%= @config[:git][:url] %>">Source code for <span class="domainname">pentandra.com</span></a>
      generously <a rel="doap:browse schema:url" href="<%= @config[:git][:browse_url] %>">hosted by GitHub</a></span>,
    with <span id="versioninfo" property="doap:release" resource="pentandra-website:release" typeof="doap:Version">this release tagged as
      <span class="tags" property="doap:revision"><a href="<%= "#{@config[:git][:tag_url]}/#{@config[:version]}" %>"><%= @config[:version] %></a></span>
      and compiled <%= @config[:production] ? "on #{render('/partials/generated_at.*', compile_time: DateTime.now)}" : "[in #{@config.unwrap.env_name} mode]" %></span>.
  </li>

  <li>
    <a rel="nofollow" href="https://www.w3.org/2012/pyRdfa/extract?uri=<%= h path_to(@item, global: true) %>"><span class="icon-screen"></span> View this web page as a machine sees it.</a>
  </li>

</ul>

<p>Made with <span class="icon-heart"><span class="visuallyhidden">love</span></span> in the <abbr>USA</abbr>.</p>
